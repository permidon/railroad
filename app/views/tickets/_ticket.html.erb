<tr>
  <td><%= ticket.number %></td>
  <td><%= link_to t('common.show'), ticket %></td>
  <td><%= link_to t('common.destroy'), ticket, method: :delete, data: { confirm: t('common.confirmation')} %></td>
</tr>
