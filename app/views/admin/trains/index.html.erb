<h1><%= t('menu.trains') %></h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th><%= t('common.number') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render @trains %>
  </tbody>
</table>

<br>

<%= link_to t('menu.new_train'), new_admin_train_path %>
